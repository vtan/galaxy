package galaxy.rendering

import galaxy.common.V2

object Paths {

  val star: Path = Path(
    V2(1, 0), V2(0.2, 0.2), V2(0, 1), V2(-0.2, 0.2),
    V2(-1, 0), V2(-0.2, -0.2), V2(0, -1), V2(0.2, -0.2)
  )
}
